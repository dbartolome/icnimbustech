# Deploy Hostinger
Compose file: `docker-compose.yml`
Deploy command: `./auto-deploy-hostinger.sh`
Variables mínimas: `POSTGRES_PASSWORD`, `MINIO_SECRET_KEY`, `SECRET_KEY`, `OLLAMA_URL`, `NEXT_PUBLIC_API_URL`.
